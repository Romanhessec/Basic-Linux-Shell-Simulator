# Basic-Linux-Shell-Simulator
Implemented a basic Linux shell simulator in C. The project included commands like ls, mkdir, touch, rm, rmdir, cd, etc. Besides the logic behind these commands, I also took care of not having memory issues such as memory leaks.
